#include <fstream>
#include <string>
#include "read_data.h"
#include "string.h"
#include "types.h"

int main(){
std::string file = ./images.txt
ReadData read = ReadData(file);
std::cout << ReadData->images_.size() << endl;

return 0;
}
